import 'package:flutter/material.dart';

class FriendsFeedPage extends StatelessWidget {
  const FriendsFeedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Friends Feed Page'),
    );
  }
}